{
  "schemaVersion": 1,
  "updated": "2026-03-07",
  "lanes": [
    {
      "id": "sd3-larger-targets",
      "status": "started",
      "scope": "Expand deterministic harness coverage beyond 512x512-equivalent smoke to larger SD3 image targets.",
      "nextAction": "Add gated 1024x1024 runtime contract fixture pack and optional CI lane."
    },
    {
      "id": "sd3-quantized-path",
      "status": "started",
      "scope": "Define and validate quantized diffusion execution contract (weights + dequantization policy + acceptance metrics).",
      "nextAction": "Pin quantized runtime schema fields and add contract tests for unsupported combinations."
    },
    {
      "id": "sd3-platform-rollout",
      "status": "started",
      "scope": "Broaden platform validation and rollout policy for browser + Node WebGPU environments.",
      "nextAction": "Add platform matrix gates for required diffusion command artifacts and pass/fail publication rules."
    }
  ],
  "exitCriteria": [
    "Each lane has contract tests in tests/config or tests/integration.",
    "Command-level verify and bench semantics remain identical across surfaces.",
    "Required diffusion artifacts are emitted in CI for each enabled lane."
  ]
}
