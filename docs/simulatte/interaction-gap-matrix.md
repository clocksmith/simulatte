# Immediate simulation interaction: verified gaps

Owner contracts: [World](../../public/simulatte/CATSCAN.md), [World application](../../public/simulatte/app/CATSCAN.md), [Motorcycle Noise](../../public/simulatte/motorcycle-noise/CATSCAN.md), and [documentation](../CATSCAN.md).

Audit date: 2026-09-23. Source revision: `8a6ad6ce66041116e060ab12f706e2b756b948b0`. The starting worktree was clean. This deliverable adds diagnosis and evidence, without changing application behavior.

The requested target is immediate, immersive interaction with a running simulation, with consequences calculated by its model. Preserve autonomous motorcycles, an onboard first-person camera, independent simulation/observation/interface state, Advanced configuration, contextual interactions, and each simulation's purpose. The supplied message establishes this target; no attachment contents were available in this session.

## Evidence boundary and route trace

All browser results below are local Chrome 136.0.7103.59 on Linux, using the repository's static server and canonical `public/` files. Desktop is 1440 × 1000; mobile is a 390 × 844 emulated viewport. This is not a physical mobile-device, hardware-GPU, deployment, acoustic-calibration, or human-review qualification.

| Route | Actual local response and source chain | Hosting contract |
|---|---|---|
| `/` | `public/index.html`; World chooser and route runtime | World target packages into `.firebase-hosting/world` |
| `/motorcycle` | `public/simulatte/motorcycle-noise/index.html`; its base URL loads `reflection-app.js`, `reflection-view.js`, `explore-ui.js`, workers, and Babylon | Explicit rewrite to the standalone entry; legacy motorcycle paths redirect here |
| `/datacenter` | `public/index.html`; `simulation-routes.js` resolves `gpu-supercluster-v1`; World loader, `world-tiers-boot.js`, native v4 plugin, and tier Canvas2D renderer | Rewrite to World entry; old full profile paths redirect here |

Authorities: [public routes](../../public/simulation-routes.js), [Firebase configuration](../../firebase.json), [local route server](../../tools/simulatte/static-site-server.mjs), [runtime manifest](../../public/simulatte/app/world-runtime-script-manifest.js). Successful request records in the browser JSON bind the two tested routes to the actual files served. Local routes use the shared route table; this does not independently verify deployed Firebase redirects. The deployed build was not inspected or modified. Runtime proof reports build ID `public-routes-20260920-v1`, which is distinct from Git HEAD.

Evidence directory: [interaction-gap-8a6ad6c](../../artifacts/interaction-gap-8a6ad6c/). [Manifest](../../artifacts/interaction-gap-8a6ad6c/manifest.json) retains source and artifact SHA-256 identities and probe paths.

## Shared lifecycle and interface gaps first

Priorities: P0 blocks a named core workflow; P1 materially prevents the requested interaction or reliable state; P2 improves evidence, clarity, or depth. “Observed” refers to this browser environment. “Source” identifies an implementation fact or unexercised risk, not a universal runtime result.

| ID / priority | Current evidence and gap | Owning source | Prioritized fix and acceptance |
|---|---|---|---|
| S1 / P1 | **Auto-start exists.** GPU launches automatically, then reaches step 4/4 and Complete. Opening Advanced preserves paused step 0; changing a parameter resets to Ready/0. This is safe configuration replacement, but there is no ongoing intervention path. | `world-tiers-boot.js:599,628`; `tier-run-controller.js:250-290` | Preserve the generation/settlement machinery. Distinguish scenario restart from model-supported live actions; issue timestamped actions to the owner model without resetting observation or interface state. Verify action causality, pause, seek, reset, stale-result rejection, and replay. Do not make every finite simulation run forever. |
| S2 / P1 | **Mobile camera fit is wrong.** At 390 px, the GPU canvas is about 403 px tall but `panY` is 425. Racks sit partly below its bottom; Reset view retains this framing. No horizontal overflow does not establish usable framing. [Screenshot](../../artifacts/interaction-gap-8a6ad6c/gpu-ui/gpu-390-initial.png). | `multi-tier-visualizer.js:865-909`, especially `height - 690` and `375 + availableHeight / 2` | Fit to the measured drawable canvas bounds. Remove obsolete page-chrome offsets from the camera calculation. Verify all required racks and useful controls at desktop/mobile sizes, resize, and Reset view without changing the run. |
| S3 / P1 | **Drawing failure can stop simulation updates while reporting Ready.** Software WebGPU motorcycle rendering throws in texture upload; time stays at 12, sound is unmeasured, and the next animation frame is never scheduled. Renderer, simulation clock, and observation updates share one unguarded callback. | `reflection-app.js:136-156`; `reflection-view.js:7-10,208-219`; `sound-view.js:31` | Isolate simulation advancement and observation from drawing failure. Publish a visible failure/recovery state outside Advanced; safely dispose failed backends. Inject render failure and verify the explicitly chosen clock policy, cancellation, cleanup, and retry. This evidence does not show failure on every WebGPU adapter. |
| S4 / P1 | **Visible datacenter motion is partly unrelated to simulation time.** Base network pulses and CDU fan glyphs consume `performance.now()` while the model is paused or Complete. This is a source-confirmed authority gap; changing pixels alone cannot prove evolving model state. | `multi-tier-visualizer.js:777-788`; `tier-renderers.js:471-537` | Bind domain motion to model time, active transfers, and modeled pump state. Keep camera/UI animation independent. Compare paused model state and domain pixels while allowing camera navigation. |
| S5 / P2 | **Advanced configuration is already implemented.** GPU's six parameters are in an Advanced drawer and nested parameter disclosure; Motorcycle uses an Advanced dialog. GPU Scenario and timeline remain in the main dock, which extends below the mobile fold. Contextual GPU rack actions are absent from the served pointer handlers. | `public/index.html:121-165,214-244`; `multi-tier-visualizer.js:134-211`; motorcycle `index.html` | Move scenario configuration into Advanced while retaining pause/resume and selected-object actions beside the scene. Add rack/link selection using existing presentation IDs and plugin inspections. Verify keyboard/touch reachability and selected-target actions without a new generic shell. |
| S6 / P2 | **Existing evidence tooling has drifted.** The shared UI audit waits for Ready even though tiers auto-start. The motorcycle audit references the old DOM/state and reads every directory entry as a file, including `vendor/`. Three navigation tests fail at HEAD. | `tools/simulatte/audit-simulation-interface.mjs`; `audit-experience-ui.mjs:61-80`; `audit-motorcycle-noise.mjs:11-13,36-52`; `tests/navigation-lifecycle.test.cjs` | Update the existing audits for the served routes and actual lifecycle, preserving pause/reset/replay assertions. Enumerate files safely. Repair test fixtures only after verifying intended behavior, including the superseded-boot failure; do not classify every failed test as harmless drift. |

## Motorcycle Noise, end to end

Purpose: observe autonomous city traffic and viewpoint-linked noise, then calculate acoustic comparisons. First person is an onboard observation camera; it is not a driving game.

| ID / priority | Verified behavior or gap | Source and evidence | Fix / acceptance |
|---|---|---|---|
| M1 / preserve | With WebGPU explicitly unavailable in the probe, WebGL draws the city, starts the default traffic automatically, updates the viewpoint dBA meter, and switches to `rider-camera`. Paused camera switching preserves simulation time; resuming moves the onboard rig. Advanced opens without pausing traffic. | [WebGL evidence](../../artifacts/interaction-gap-8a6ad6c/forced-webgl/motorcycle-browser.json), [city](../../artifacts/interaction-gap-8a6ad6c/forced-webgl/motorcycle-1440-initial.png), [onboard](../../artifacts/interaction-gap-8a6ad6c/forced-webgl/motorcycle-1440-onboard.png); `reflection-view.js:29-31,154-174,208-217` | Preserve these improvements. Do not replace autonomous motion with player steering, reset traffic on camera changes, or move contextual source actions into configuration. WebGL was forced through a browser capability override; this is not proof that automatic fallback works. |
| M2 / P0 | **Compare snapshot fails.** The browser reports `FFT requires equal power-of-two arrays`, leaves results hidden, and cannot enable comparison export. A reduced two-motorcycle case exposes the same contract failure without the cost of a default 300-motorcycle waveform calculation. | `reflection-worker.js:52-55` passes `8000` as the second argument to `signal.js:32-35`, where it means FFT length, not sample rate. `signal.js:7-9` rejects that length. [Browser evidence](../../artifacts/interaction-gap-8a6ad6c/forced-webgl/motorcycle-browser.json), [direct reproduction](../../artifacts/interaction-gap-8a6ad6c/spectrum-contract.txt). | Fix the worker-to-signal contract: choose a valid FFT length and explicitly derive frequency/power bins. The worker also expects `bins`, but `spectrum()` returns `{real, imaginary}`; changing only 8000 would leave spectral observation broken. Test a known tone and mixed signal, then browser compare/cancel/export/import at fixed time and observer, including the default population. |
| M3 / P1 | **Backend startup and runtime recovery are incomplete.** Without unsafe WebGPU enabled, this Chrome exposes `navigator.gpu` but startup reports `Cannot read properties of undefined (reading 'dispose')`. With software WebGPU enabled, texture upload throws repeatedly and kills the application tick. | [Startup attempt](../../artifacts/interaction-gap-8a6ad6c/webgl/motorcycle-browser.json), [WebGPU attempt](../../artifacts/interaction-gap-8a6ad6c/motorcycle-browser.json); `reflection-view.js:8` disposes a partially initialized engine inside its fallback catch | Qualify backend creation, cleanup, first-frame success, and runtime loss separately. Failed disposal must not prevent the fallback attempt. Keep failure status visible on the scene. Test absent GPU API, adapter rejection, partially initialized engine, and first-frame upload failure. Physical hardware verification remains separate. |
| M4 / P1 | **Replay has an observation-authority risk beyond the blocked export.** Import restores `scene.receiver`, then invokes `run()`, which replaces the analysis receiver with the current camera observer. Import does not restore that camera first and does not compare the supplied record with the recomputed result. This is source-grounded; the FFT failure blocks a normal exported-bundle browser round trip. | `reflection-app.js:86-97,120-134` | Retain the exported observation snapshot explicitly and execute against it. Validate independent replay identity and numerical outputs before reporting reproduction. Test importing after moving to a different camera, modified result records, and geometry/model mismatches. Live observer movement remains independent of traffic. |
| M5 / P2 | Camera changes request new observer samples, but responses are checked against request ID/epoch, not the current observer identity. While a sample is pending, changing camera can temporarily show the previous viewpoint's reading; its timestamp/position is in Advanced. | `explore-ui.js:39-61,79-90,109-118` | Bind displayed readings to observer and simulation-time identity. Mark a stale reading or show pending immediately on viewpoint changes. Delay worker responses in a regression and verify old readings cannot claim to be at the new viewpoint. |

The normal end-to-end path currently stops at comparison failure. Audio audibility, treatment placement, default-population waveform completion, successful export/import, and physical acoustic accuracy are not established by these probes. Existing older acoustic tests do not qualify the current NYC integration; its charter explicitly says so.

## GPU Supercluster, end to end

Purpose: explore collective communication, stragglers, coolant flow, temperature, throttling, and utilization across the governed 32-rack/256-GPU topology. The useful existing model should remain the authority for consequences.

| ID / priority | Verified behavior or gap | Source and evidence | Fix / acceptance |
|---|---|---|---|
| G1 / preserve | Route loads the native v4 plugin, auto-starts, settles, pauses/replays, applies controls, and retains results on reload. Desktop coolant flow 120 → 20 L/min changes peak temperature 37 → 57.3 C and PUE 1.746 → 1.583. Mobile starts from the retained 20 L/min result; changing to 30 gives 50.6 C. These are model outputs. Six controls become visible when Advanced Parameters is expanded. | [Corrected desktop/mobile probe](../../artifacts/interaction-gap-8a6ad6c/gpu-ui/gpu-browser.json), [Advanced](../../artifacts/interaction-gap-8a6ad6c/gpu-ui/gpu-1440-advanced.png); `index.js:14-93,169-211`; `thermal-model.js` | Preserve normalization, topology, model calculations, controls, generation checks, settlement, comparisons, and replay infrastructure. Both viewport flows completed without page exceptions. DOM-driven controls are browser behavior evidence, not proof of every physical input gesture. |
| G2 / P1 | **The model does not evolve through the four stages.** `simulate()` computes the result at activation/start. Each subsequent step increments `currentStep`; it does not integrate heat or advance network queues. The implementation truthfully declares `deterministic-result-replay`. This falls short of the new running-simulation target. | `index.js:181-211`; `v4-contribution.js:17-22,111-150`; `thermal-model.js:20-95`; browser step 4/4 | Extend this plugin with bounded deterministic simulation steps and a model-owned intervention log. Use existing collective and thermal calculations where appropriate; introduce explicit transient state where the requested behavior needs it. Verify a fault at time t affects later barriers/temperature and that replay reproduces the trajectory. Auto-looping the four stages is insufficient. |
| G3 / P1 | **Rack/link contextual interaction is incomplete.** The native v4 contribution exposes a cluster-wide inspection. Canvas pointer handlers pan and zoom; they do not pick rack/link IDs. The older semantic presentation's `pickBehavior` strings do not prove a served selection path. | `v4-contribution.js:151-177`; `multi-tier-visualizer.js:134-211`; contrast `presentation.js:85-111` and experience documentation's selection claims | Extend the existing canvas/presentation interaction path with hit testing and selected rack/link inspection. Apply only model-supported local actions through plugin controls/actions. Verify selected IDs, changed quantities, visual effects, and unchanged unrelated state. This row is source-confirmed, not a physical rack-click browser test. |
| G4 / P2 | **Replay and visual evidence are different.** Exact WorldSpec replay was exercised on a fresh desktop baseline: replay pass, matching outcomes, overall WorldProof `not-proven` because visual obligations remain unproven. Ordinary replay/reload also retains intervention action results. | [Exact replay receipt](../../artifacts/interaction-gap-8a6ad6c/gpu-exact-replay.json); `profile-program.js:114-140` | Preserve the honest proof classes. Retain exact intervention replay, export/reimport, bound screenshots, and human visual review after the interaction fixes. Ordinary Replay does not automatically run the independent WorldSpec replay proof. |

The actual renderer receipt is `canvas2d`, even when the shared audit requests software WebGPU. Neither the product name nor the browser flag proves GPU rendering or physical GPU simulation. The current [experience page](experiences/gpu-supercluster.md) correctly describes a two-pass static thermal scenario model; the [older improvement ledger](experiences/improvements/gpu-supercluster.md) still overstates thermodynamic ODEs and launch qualification.

## Recommended implementation order

1. Repair shared lifecycle failure publication and canvas fitting (S2/S3), retaining independent simulation, observation, and interface state. Update the stale browser audit assumptions as part of the relevant regression coverage (S6).
2. Repair Motorcycle's comparison API boundary completely (M2), then verify backend recovery and observation-bound replay (M3/M4). Preserve and regression-test the existing autonomous/onboard/Advanced improvements (M1).
3. Extend GPU Supercluster's model-owned time evolution and interventions (S1/G2), then bind visual motion and contextual rack/link interactions to that state (S4/G3). Keep its collective/thermal purpose and existing plugin lifecycle.
4. Complete Advanced configuration placement, visible measurement freshness, and evidence qualification (S5/M5/G4).

Before implementing the continuous-model change, reflect the user's explicit interaction target in `GOALS.md` and affected charters. Retain the hexagon chooser, static hosting, distinct products, fixed compiler phase order, and truthful finite experiments. Do not redesign unrelated simulations or replace existing infrastructure with a universal shell.

## Validation and retained limitations

| Check | Result |
|---|---|
| `node --test tests/gpu-supercluster.test.cjs tests/tier-run-controller.test.cjs tests/navigation-lifecycle.test.cjs tests/motorcycle-noise.test.cjs` | 80 tests: 77 pass, 3 fail. [Log](../../artifacts/interaction-gap-8a6ad6c/focused-tests.log). All failures are in navigation lifecycle: moved Step control assertion, old canonical route expectation, and superseded-boot fixture accessing undefined `signal`. The last requires separate diagnosis. |
| `node tools/simulatte/audit-simulation-interface.mjs --profile gpu-supercluster-v1 --software` with retained output directory | Fails both viewport cases waiting for Ready; captures completed Canvas2D state. [Report](../../artifacts/interaction-gap-8a6ad6c/shared-gpu/report.json). |
| Fresh GPU UI and exact-replay probes | Desktop/mobile configuration, pause, replay, reload, and reset-view flow captured; separate baseline exact replay passes. Mobile framing defect remains. |
| Fresh motorcycle probes | Software WebGPU runtime failure, automatic-fallback startup failure, and explicitly forced WebGL interaction/comparison results retained separately. |
| `npm run catscan:check` | Pass: 42 charters and synchronized index. [Log](../../artifacts/interaction-gap-8a6ad6c/catscan-check.log). |

The initial GPU probe used an unchanged control value on its mobile pass, then incorrectly waited for a new run after clicking hidden Start while paused. Its timeout is a probe error, not a product finding. `gpu-ui/` is the corrected flow: it expands parameter controls, changes the value, closes the drawer, and starts the new run. The initial screenshots also captured a transient loading overlay; framing conclusions use the corrected post-loading screenshots. The unavailable-backend motorcycle probe was terminated after its startup failure; later attempted interactions in that artifact are not valid feature checks.

All probes reuse the repository browser harness and static server. Temporary browser profiles and verification servers are closed after capture. No deployment or application implementation changes were made.

Component: documentation, with evidence for World application, Motorcycle Noise, and GPU Supercluster.
Intent: preserved in runtime; the user's requested interaction target is recorded separately from current behavior.
Acceptance evidence: commands, receipts, screenshots, and source/artifact manifest linked above.
Boundary effects: documentation and audit artifacts only.
