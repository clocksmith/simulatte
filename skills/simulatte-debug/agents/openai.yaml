interface:
  display_name: "Simulatte Debug"
  short_description: "Diagnose a named Simulatte failure"
  default_prompt: "Use $simulatte-debug to reproduce and diagnose this failure in Simulatte; repair it only if requested."
